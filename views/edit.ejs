<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Blog Application</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex w-full justify-end">
            <a class="bg-red-600  text-sm mb-5 inline-block rounded-md px-3 py-2" href="/logout">Logout</a>
        </div>
        
        <h5 class="mb-5">Edit your post</h5>
        <form action="/update/<%= post._id %>" method="post">
            <textarea placeholder="What's on your Mind ?" class="block  p-3 outline-none w-1/3 resize-none border-zinc-800 border-2 rounded-md bg-transparent" name="content"><%= post.content %></textarea>
            <input class="px-3 w-40 py-2 bg-yellow-500 text-black font-bold rounded-md text-sm mt-2 block" type="submit" value="Update Post">
        </form>
        
        
    </div>
    
</body>
</html>